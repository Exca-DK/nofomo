[package]
name = "tempo-agentic-price-dexpaprika"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
futures.workspace = true
# Declared for its features, not its API: `reqwest-eventsource` asks only for
# `stream`, and under resolver v2 features unify per build. Building this crate
# without a sibling that enables TLS would otherwise produce a client that
# rejects https with "scheme is not http".
reqwest.workspace = true
reqwest-eventsource.workspace = true
serde_json.workspace = true
tempo-agentic-price.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
tracing-subscriber.workspace = true

[lints]
workspace = true
