{
  // Run `make schema` after cloning so sqlx validates against current migrations.
  "rust-analyzer.cargo.extraEnv": {
    "DATABASE_URL": "sqlite:///tmp/tempo-agentic-sqlx.db"
  },
  "rust-analyzer.check.extraEnv": {
    "DATABASE_URL": "sqlite:///tmp/tempo-agentic-sqlx.db"
  },
  "wake.compiler.solc.remappings": []
}
